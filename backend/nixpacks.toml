[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r ../requirements.txt"]

[start]
cmd = "PYTHONPATH=/app/..:${PYTHONPATH:-} uvicorn main:app --host 0.0.0.0 --port $PORT --log-level info"
